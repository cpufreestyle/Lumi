#!/bin/bash
set -e
cd "/Users/a1-6/AI Shared/repo/Lumi"
gh release create v1.1.6 \
  --title "v1.1.6 新增检查更新（自动检测新版本）" \
  --notes "## 新增内容
- **检查更新**：应用启动后自动静默检查 GitHub 最新 Release，并可在展开面板右上角手动点击检查。
- **更新提示**：发现新版本时，面板顶部弹出提示横幅，显示「新版本 vX.X.X 可用 / 当前版本」，提供「前往下载」（打开 Release 页）与「忽略此版本」。
- **状态反馈**：检查更新按钮图标随状态变化——检查中转圈、有更新变绿、已是最新显示✓、失败显示⚠，悬停可见详细说明。
- **忽略版本**：可忽略某一版本，不再弹窗提示（记忆在本机）。

## 说明
本版本为轻量自研更新检测（读取 GitHub Release）。因当前安装包未签名，暂未接入 Sparkle 后台自动安装；检测到新版本后会引导前往 Release 页下载替换。后续完成签名公证后可平滑升级为全自动更新。

## 受影响版本
- 此前版本（含 v1.1.5）均无更新检测，建议升级到 v1.1.6。

## 安装
下载 Lumi-v1.1.6.zip，解压后将 Lumi.app 拖入「应用程序」并打开（菜单栏常驻）。" \
  Lumi-v1.1.6.zip#Lumi-v1.1.6.zip
echo "DONE_RELEASE"
